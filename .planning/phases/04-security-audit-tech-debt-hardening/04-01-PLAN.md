---
phase: 04-security-audit-tech-debt-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/routes/api.ts
  - backend/src/dispatch/soloDispatch.ts
  - backend/src/routes/api.errorHandling.test.ts
  - backend/src/routes/api.sessionPersistence.test.ts
autonomous: true
requirements:
  - SEC-05
  - DEBT-06
must_haves:
  truths:
    - "Errors logged in dispatch paths completely omit authorization headers and request body payloads"
    - "GET /sessions API route enforces a limit clause on database fetches and supports limit parameter"
  artifacts:
    - path: "backend/src/routes/api.ts"
      provides: "Sanitized error logging helper and GET /sessions limit parameter validation"
    - path: "backend/src/dispatch/soloDispatch.ts"
      provides: "Sanitized error output during fallback logic"
  key_links:
    - from: "backend/src/routes/api.ts"
      to: "free_council.db"
      via: "db.prepare().all()"
      pattern: "LIMIT"
---

<objective>
Harden the backend dispatch paths by redacting API keys from error logs and optimize session querying by adding a customizable LIMIT query parameter that is pushed down directly into the database engine.

Purpose: Prevent security leakage of client/OpenRouter keys in server logs and avoid performance degradation from unbounded session history queries.
Output: Redacted logs in routes and dispatchers, updated GET /sessions SQL query, and accompanying tests.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/src/routes/api.ts
@backend/src/dispatch/soloDispatch.ts

<interfaces>
From backend/src/routes/api.ts:
```typescript
export function reportDispatchError(res: Response, context: 'council' | 'solo', err: unknown, partial: boolean): void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and sanitize logging paths in api.ts and soloDispatch.ts</name>
  <files>backend/src/routes/api.ts, backend/src/dispatch/soloDispatch.ts</files>
  <read_first>
    - backend/src/routes/api.ts
    - backend/src/dispatch/soloDispatch.ts
  </read_first>
  <action>
    Introduce a utility helper function in `backend/src/routes/api.ts` to sanitize error objects for logging. The helper must:
    1. Check if the error is an instance of Error, and extract its message.
    2. Redact any occurrence of "Bearer <token>" or "sk-<key>" or any authorization-like tokens from the error message.
    3. Ensure we do NOT pass the raw error object (or Axios/Fetch config containing headers) directly to `console.error`.
    Update `reportDispatchError` in `backend/src/routes/api.ts` to log using this sanitizer:
    `console.error(\`[dispatch:\${context}] Request failed:\`, sanitizeErrorForLogging(err));`
    Update the `console.error` inside `dispatchSoloChat` in `backend/src/dispatch/soloDispatch.ts` (around line 371) to also log only the sanitized message:
    `console.error(\`[soloDispatch] Attempt \${attempts} failed:\`, sanitizeErrorForLogging(err));`
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    No console.error calls in dispatch paths log raw HTTP config/headers. Custom error tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement query limits in GET /sessions SQL query</name>
  <files>backend/src/routes/api.ts</files>
  <read_first>
    - backend/src/routes/api.ts
  </read_first>
  <action>
    Modify the `GET /sessions` route handler in `backend/src/routes/api.ts` to support limit pagination (per D-04):
    1. Parse `limit` from `req.query.limit` as a base-10 integer:
       `let limit = parseInt(String(req.query.limit || '50'), 10);`
    2. Add fallback validation: if `limit` is NaN or less than/equal to 0, default it to 50.
    3. Update the SQLite prepared statement:
       ```sql
       SELECT c.id, c.ts
       FROM conversations c
       JOIN sessions s ON c.id = s.id
       WHERE s.owner_api_key_hash = ?
       ORDER BY c.ts DESC
       LIMIT ?
       ```
    4. Bind `ownerHash` and `limit` to the database query:
       `const rows = db.prepare(...).all(ownerHash, limit);`
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    GET /sessions SQLite query accepts and binds the limit parameter, defaulting to 50.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write tests for log redaction and sessions limit</name>
  <files>backend/src/routes/api.errorHandling.test.ts, backend/src/routes/api.sessionPersistence.test.ts</files>
  <read_first>
    - backend/src/routes/api.errorHandling.test.ts
    - backend/src/routes/api.sessionPersistence.test.ts
  </read_first>
  <action>
    1. Update the `reportDispatchError` test in `backend/src/routes/api.errorHandling.test.ts` to mock `console.error` and verify that an error containing headers/tokens (e.g. `Authorization: Bearer my-secret-api-key`) is redacted.
    2. Add a test in `backend/src/routes/api.sessionPersistence.test.ts` for the `GET /sessions` limit parameter:
       - Populate 3 test sessions for a mock owner.
       - Send a request to `/sessions?limit=2` and assert that only the 2 most recent sessions are returned.
       - Send a request to `/sessions` (without limit) and assert that all 3 are returned (default limit is 50).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    New tests compile and pass successfully, proving log redaction and sql limit parameters work.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Server | Client inputs (query parameters, authorization headers) cross here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | `console.error` logs | mitigate | Sanitize all error logs in dispatch paths to redact Bearer tokens and API keys. |
| T-04-02 | Denial of Service | `GET /sessions` SQLite query | mitigate | Apply strict integer parsing and upper bounds on SQL LIMIT values. |
</threat_model>

<verification>
Verify error redaction and session limits:
- Mock fetch errors with API keys in headers to verify console redaction.
- Perform HTTP calls to GET /sessions with custom limits to verify subset returns.
</verification>

<success_criteria>
- [ ] console.error redact functions are wired and verified.
- [ ] SQL LIMIT bindings prevent full table scans on large sessions lists.
- [ ] No regression on baseline test coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/04-security-audit-tech-debt-hardening/04-01-SUMMARY.md`
</output>
