---
phase: 04-security-audit-tech-debt-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/agents/AgentOrchestrator.ts
  - backend/src/server/gracefulShutdown.ts
  - backend/src/index.ts
  - frontend/src/components/CouncilTrace.tsx
  - backend/src/agents/AgentOrchestrator.test.ts
  - backend/src/server/gracefulShutdown.test.ts
autonomous: true
requirements:
  - DEBT-04
  - DEBT-05
must_haves:
  truths:
    - "Proposer cache checks use prompt hashes computed via crypto SHA-256"
    - "ProviderHealthMonitor starts on server init and is stopped on graceful shutdown"
    - "Trace viewer displays a 'cached' badge for cache hits"
  artifacts:
    - path: "backend/src/agents/AgentOrchestrator.ts"
      provides: "SHA-256 caching and fromCache metadata yields"
    - path: "backend/src/server/gracefulShutdown.ts"
      provides: "Stopping ProviderHealthMonitor during shutdown"
    - path: "frontend/src/components/CouncilTrace.tsx"
      provides: "Cached badge next to proposer agent models"
  key_links:
    - from: "backend/src/index.ts"
      to: "backend/src/server/gracefulShutdown.ts"
      via: "installGracefulShutdown"
---

<objective>
Fix the cache key collision vulnerability in the proposer caching mechanism, clean up server lifecycle hooks by wiring ProviderHealthMonitor into the graceful shutdown installer, and enhance visual observability by showing a "cached" badge in the trace panel.

Purpose: Eliminate cache collisions for similar-length prompts, prevent database write-after-close errors during server shutdown, and show trace caching metrics visually in the UI.
Output: Crypto-hashed cache keys, updated startup/shutdown lifecycles, and a React caching indicator badge.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/src/agents/AgentOrchestrator.ts
@backend/src/server/gracefulShutdown.ts
@frontend/src/components/CouncilTrace.tsx

<interfaces>
From backend/src/modules/providerHealthMonitor.ts:
```typescript
export const ProviderHealthMonitor = {
  start(intervalMs?: number): void;
  stop(): void;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace proposer cache key with SHA-256 and return fromCache in generator yields</name>
  <files>backend/src/agents/AgentOrchestrator.ts</files>
  <read_first>
    - backend/src/agents/AgentOrchestrator.ts
  </read_first>
  <action>
    1. Import `crypto` from `'node:crypto'` in `backend/src/agents/AgentOrchestrator.ts`.
    2. Replace the body of `getCacheKey(modelId: string, prompt: string)`:
       ```typescript
       function getCacheKey(modelId: string, prompt: string): string {
         const hash = crypto.createHash('sha256').update(prompt).digest('hex');
         return `${modelId}|${hash}`;
       }
       ```
    3. Update the generator yields for proposer completions inside `executeGoAMoAHybrid` to include the `fromCache` boolean (per D-03):
       ```typescript
       // Inside executeGoAMoAHybrid proposer completion loop
       yield {
         role: outcome.agent.role,
         modelId: outcome.agent.modelId,
         response: outcome.text,
         status: 'completed',
         fromCache: outcome.fromCache // Return fromCache metadata
       };
       ```
    4. Ensure the final `Synthesizer` completion or trace object includes the `fromCache` properties as needed in `AgentOrchestrator.ts`.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    Proposer cache key checks use SHA-256 prompt hashing and the generator outputs `fromCache` metadata.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire ProviderHealthMonitor lifecycle into server startup and graceful shutdown</name>
  <files>backend/src/server/gracefulShutdown.ts, backend/src/index.ts</files>
  <read_first>
    - backend/src/server/gracefulShutdown.ts
    - backend/src/index.ts
  </read_first>
  <action>
    1. Update the `ShutdownDeps` interface in `backend/src/server/gracefulShutdown.ts` to include `providerHealthMonitor?: { stop: () => void }` (per D-02).
    2. Inside the `shutdown` function of `installGracefulShutdown`, invoke `providerHealthMonitor?.stop()` immediately after `retentionMonitor.stop()`.
    3. In `backend/src/index.ts`, import `ProviderHealthMonitor` from `'./modules/providerHealthMonitor.js'`.
    4. Start it on server boot: call `ProviderHealthMonitor.start();` inside the `startServer` callback where other monitors start.
    5. Pass `providerHealthMonitor: ProviderHealthMonitor` to `installGracefulShutdown({...})` in `index.ts`.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    ProviderHealthMonitor is started on boot and stopped on shutdown signals.
  </done>
</task>

<task type="auto">
  <name>Task 3: Render 'cached' badge next to proposer agent models in trace panel</name>
  <files>frontend/src/components/CouncilTrace.tsx</files>
  <read_first>
    - frontend/src/components/CouncilTrace.tsx
  </read_first>
  <action>
    1. Update `TraceAgent` interface in `frontend/src/components/CouncilTrace.tsx` to include `fromCache?: boolean;`.
    2. Locate the proposer agents loop (`trace.agents?.filter(a => a.role !== 'Aggregator').map((agent, i) => ...`) in the JSX of `CouncilTrace.tsx`.
    3. Render a visual badge next to the agent's name/model when `agent.fromCache` is true:
       ```typescript
       {agent.fromCache && (
         <span className="text-[9px] px-1.5 py-0.2 rounded-full bg-violet-600/20 text-violet-300 border border-violet-500/30 font-bold tracking-tight uppercase">
           cached
         </span>
       )}
       ```
       Ensure it aligns properly within the layout (similar to other capability badges).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    Frontend builds successfully and the trace panel displays the cached badge on cache hits.
  </done>
</task>

<task type="auto">
  <name>Task 4: Write tests for caching SHA-256 and ProviderHealthMonitor shutdown</name>
  <files>backend/src/agents/AgentOrchestrator.test.ts, backend/src/server/gracefulShutdown.test.ts</files>
  <read_first>
    - backend/src/agents/AgentOrchestrator.test.ts
    - backend/src/server/gracefulShutdown.test.ts
  </read_first>
  <action>
    1. In `backend/src/agents/AgentOrchestrator.test.ts`, add a test verifying that proposer cache hits yield the `fromCache: true` metadata, and that cache misses yield `fromCache: false`.
    2. In `backend/src/server/gracefulShutdown.test.ts`, add a test to verify that `providerHealthMonitor.stop()` is called when a shutdown signal is simulated.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    All caching and graceful shutdown unit tests pass successfully.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries crossed.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Denial of Service | Proposer caching | mitigate | Replace weak length+prefix hashing with cryptographic SHA-256. |
</threat_model>

<verification>
Verify prompt cache hits and graceful monitor teardown:
- Run the test suite using `npm test`.
- Trigger a build with `npm run build` in the frontend directory.
</verification>

<success_criteria>
- [ ] SHA-256 prompt hashing is fully covered by unit tests.
- [ ] ProviderHealthMonitor teardown logic prevents database writes post-close.
- [ ] CouncilTrace builds without compilation errors.
</success_criteria>

<output>
After completion, create `.planning/phases/04-security-audit-tech-debt-hardening/04-02-SUMMARY.md`
</output>
